[workspace]
members = ["syncer_wasm", "syncer", "sia_sdk", "sia_derive"]
resolver = "2"

# CPU-intensive operations (hashing, encryption) are ~100x slower
# without optimisations even in dev builds.
[profile.dev.package.sia_sdk]
opt-level = 3

[profile.dev.package.sia_syncer]
opt-level = 3
